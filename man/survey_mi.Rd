% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_mi.R
\name{survey_mi}
\alias{survey_mi}
\title{Simple survey MI analysis: one imputation per bootstrap replicate}
\usage{
survey_mi(
  design,
  analysis,
  formulas = NULL,
  variables = NULL,
  methods = NULL,
  analysis_family = c("gaussian", "binomial", "poisson"),
  B = 100L,
  n_rounds = 10L,
  seed = NULL,
  fitters = NULL
)
}
\arguments{
\item{design}{A \code{\link[survey:svydesign]{survey::svydesign()}} object.}

\item{analysis}{Two-sided formula for the substantive model (e.g. \code{y ~ x1 + x2}).}

\item{formulas, variables, methods}{Imputation specification; same rules as
\link{survey_mi} (provide \code{formulas} or \code{variables} + \code{methods}).}

\item{analysis_family}{\code{"gaussian"}, \code{"binomial"}, or \code{"poisson"} for the
substantive model fit.}

\item{B}{Number of multinomial bootstrap replicate columns from
\code{\link[=multinom_weights]{multinom_weights()}}. Defaults to \code{100}.}

\item{n_rounds}{Number of chained-equation rounds for each imputation.}

\item{seed}{Optional seed for reproducible bootstrap weights and imputation draws.}

\item{fitters}{Optional fitter overrides; same as \link{survey_mi}.}
}
\value{
A list with:
\describe{
\item{coefs}{matrix (\verb{B x p}) of coefficient estimates by bootstrap replicate.}
\item{coef_names}{coefficient names.}
\item{summary}{data frame with \code{term}, \code{estimate}, \code{conf.low}, \code{conf.high}.}
\item{B}{number of bootstrap replicates used.}
}
}
\description{
Draws multinomial bootstrap replicate weights \verb{Wt_1, ..., Wt_B}. For each
replicate \code{b}, imputes missing values once using \code{Wt_b}, fits the substantive
weighted model, and stores one coefficient vector. Returns all \code{B} estimates
plus an empirical summary (mean and percentile 95\% interval).
}
