% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multinom_weights.R
\name{multinom_weights}
\alias{multinom_weights}
\title{Generate multinomial bootstrap replicate weights by stratum}
\usage{
multinom_weights(design, B = 50, normalize = TRUE)
}
\arguments{
\item{design}{A survey design object containing strata, PSU identifiers, and
base sampling weights.}

\item{B}{Integer number of bootstrap replicates to generate.}

\item{normalize}{If \code{TRUE} (default), each replicate column is divided by its
unweighted sample mean so replicate weights have mean one, following the
Kim-style bootstrap (divide each replicate by its unweighted mean).}
}
\value{
A data frame with one row per original observation, including stratum
and PSU identifiers plus replicate-weight columns \code{Wt_1} to \code{Wt_B}.
}
\description{
Builds bootstrap replicate weights for a complex survey design using
stratum-specific multinomial PSU resampling with size \code{n_h - 1} and equal
selection probabilities, then rescales by \code{n_h / (n_h - 1)}.
}
\examples{
design <- svydesign(
  id = ~cluster_id,
  strata = ~strata_id,
  data = mydata,
  weights = ~weight_var,
  nest = TRUE
)

w_boot <- multinom_weights(design, B = 200)

}
