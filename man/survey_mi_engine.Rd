% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_mi.R
\name{survey_mi_engine}
\alias{survey_mi_engine}
\title{Sequential weighted imputation for complex surveys}
\usage{
survey_mi_engine(
  design,
  formulas = NULL,
  ...,
  weights = NULL,
  variables = NULL,
  methods = NULL,
  B = NULL,
  n_rounds = 10,
  n_imputations = NULL,
  seed = NULL,
  fitters = NULL,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{design}{Either a survey design (\code{inherits(, "survey.design")}) —
typically from \code{\link[survey:svydesign]{survey::svydesign()}} — \strong{or} a \code{data.frame} (legacy usage).}

\item{formulas}{Explicit conditional specifications (optional): a named list
grouping models by family (\code{gaussian}, \code{binomial}, \code{poisson}), each entry one
formula or list of formulas, LHS a single bare name. Omit this if you supply
\strong{\code{variables} + \code{methods}} instead (PROC MI–style VAR + method).

Do not pass \strong{\code{formulas}} together with \strong{\code{variables}} — choose one style.}

\item{...}{Currently unused (reserved).}

\item{weights}{Analysis weights (\code{data.frame} path only).}

\item{variables}{\strong{PROC MI–style VAR list.} Character vector of analysis
variables that enter the imputation (complete and incomplete). For each
incomplete \code{y} among these names, a model is built automatically as
\verb{y ~ <all other names in variables>} (additive main effects only).
\strong{Only these columns} are passed to the imputation engine; all other
columns in \code{design}/\code{data} are ignored for fitting. \strong{Returned imputations
are data frames with exactly these columns} (same row order as the survey
design). Add any auxiliary predictors (e.g. stratification controls) to
\code{variables} if you want them in the model.}

\item{methods}{\strong{With \code{variables} (PROC MI–style):} named list \code{list(gaussian = ..., binomial = ..., poisson = ...)} with entries as character vectors.
List \strong{only incomplete variables} that need imputation, \strong{each exactly once}
across the three families. Omit a family (or set it to \code{character(0)} or
\code{NULL}) when unused.

\strong{\code{data.frame}, \code{formulas = NULL}, \code{variables = NULL}:} legacy behaviour
over all missing columns in the full frame (see \code{.resolve_methods()}).}

\item{B}{Number of multinomial bootstrap replicate weight columns from
\code{\link[=multinom_weights]{multinom_weights()}} (\strong{survey designs only}). Defaults to \strong{\code{100}} when
\strong{\code{NULL}} (with \strong{\code{n_imputations}} defaulting to \strong{\code{5}}).}

\item{n_rounds}{Rounds \code{c} in Raghunathan et al..}

\item{n_imputations}{Number of stochastic MI draws \strong{per bootstrap replicate}
(\strong{survey:} denoted \strong{\code{A}}; total completed datasets = \strong{\code{B * A}}). Defaults
to \strong{\code{5}} when \strong{\code{NULL}} on survey inputs. For a plain \strong{\code{data.frame}},
this is the total number of imputations (same default \strong{\code{5}} when \strong{\code{NULL}}).}

\item{seed}{Optional; passed to \code{\link{set.seed}} prior to RNG use.}

\item{fitters}{Optional overrides for \code{gaussian}, \code{binomial}, \code{poisson} fitters.}

\item{parallel}{If \strong{\code{TRUE}} (survey designs only, with \strong{\code{block_info}}),
run bootstrap blocks on parallel workers via \strong{\link[parallel:mclapply]{parallel::mclapply}} (Unix/macOS
fork only). Windows falls back to sequential with a message.}

\item{n_cores}{Maximum worker processes when \strong{\code{parallel}} is \strong{\code{TRUE}};
default \strong{\code{detectCores()-1}} (minimum \strong{\code{1}}).}
}
\value{
List of completed \code{data.frame}s: length \strong{\code{n_imputations}} (plain
\strong{\code{data.frame}}), or \strong{\code{B * A}} for survey designs ( \strong{\code{A}} =
\strong{\code{n_imputations}} per \strong{\code{Wt_b}}). Attributes \strong{\code{B}}, \strong{\code{A}}, and
\strong{\code{bootstrap_rep}} map each list element to its replicate index \strong{\code{b}}.
}
\description{
Sequential chained equations (least to most missing) with multinomial PSU
bootstrap replicate weights from \code{\link[=multinom_weights]{multinom_weights()}} whenever \code{design}
inherits \code{"survey.design"}.
}
\keyword{internal}
